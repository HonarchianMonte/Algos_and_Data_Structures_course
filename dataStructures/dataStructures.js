1
'strings'
booleans
undefined

[] array
{} object 

//Actions performed with Data Structures
// Insertion, Deletion, Traversal(accessing each data item once to be processed)
// ,Searching, Sorting, Access

// lookup O(1)
// push O(1) * can be O(n)
// insert O(n)
// delete O(n)